// source code
var calculator = {
	sum : function(a, b) {'use strict';
		return a + b;
	},

	multiply : function(a, b) {'use strict';
		return a * b;
	},

	subtract : function(a, b) {'use strict';
		return a - b;
	}
};
