These files would typically belong in your cgi-bin folder. For instance, your
path to them might look like this:

./cgi-bin/cscchords/GetChords.py

On a typical Linux set up, you would have this:

/usr/lib/cgi-bin/cscchords/GetChords.py